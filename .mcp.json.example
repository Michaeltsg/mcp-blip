{
  "mcpServers": {
    "blip": {
      "command": "npx",
      "args": ["-y", "take-blip-mcp"],
      "env": {
        "BLIP_ENV_FILE": "blip.env",
        "BLIP_ALLOW_WRITES": "false"
      }
    }
  }
}
